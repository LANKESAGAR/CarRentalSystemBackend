spring.application.name=carrentalsystem
server.servlet.context-path=/carRentalSystem

# Database Configuration - Read from Environment Variables
# These will be set on the hosting platform (e.g., Render)
spring.datasource.url=jdbc:postgresql://localhost:5432/CarRentalSystem
spring.datasource.username=postgres
spring.datasource.password=Mrudhula@4C8
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Settings
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
#spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT Configuration - Read from Environment Variables
jwt.secret=${JWT_SECRET_KEY:d092fc9caf9e88b7173bce622b0705b3ba9bc036f29b43d6583c51aeba8a896d}
jwt.expirationMs=${JWT_EXPIRATION_MS:3600000}

# Frontend URL for CORS - Read from Environment Variable
# This URL will be provided by your deployed frontend (e.g., Netlify)
frontend.url=${FRONTEND_URL:http://localhost:5173}
